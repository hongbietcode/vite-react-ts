import { Input } from 'antd';
import { FC } from 'react';

const LoginPage: FC = () => {
    return (
        <div>
            <Input />
            <h1>Login</h1>
        </div>
    );
};

export default LoginPage;
