import { IRouter } from '@src/types/utils';

export const AUTH_ROUTERS: IRouter[] = [
    {
        path: '/login',
        page: 'LoginPage',
        title: 'Đăng nhập',
    },
];

export const APP_ROUTERS: IRouter[] = [];
